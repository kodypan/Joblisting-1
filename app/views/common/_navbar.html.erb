<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluld">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/"><img src='<%= image_url 'logo2.png' %>' >Voffer</a>

  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <form class="navbar-form navbar-right">
      <div class="form-group">
        <input type="text" class="form-control" placeholder="Search A Offer">
      </div>
      <button type="submit" class="btn btn-default">搜索职位</button>
    </form>
    <ul class="nav navbar-nav navbar-right">
      <li><%= link_to("For Company企业用户", admin_jobs_path) %> </li>
      <% if !current_user %>


              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">个人用户 <span class="caret"></span></a>
                <ul class="dropdown-menu">
              <li><%= link_to("注册 signup", new_user_registration_path) %></li>
                <li role="separator" class="divider"></li>
              <li><%= link_to("登陆 login", new_user_session_path) %></li>




              <%else%>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  Hi!, <%= current_user.email %>
                  <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
            <% if current_user.admin? %>
                  <li> <%= link_to("管理平台Admin Panel", admin_jobs_path) %> </li>
            <% end %>
                  <li> <%= link_to("注销logout", destroy_user_session_path,method: :delete) %></li>
                  </ul>
                </li>
            <%end%>

    </ul>
  </div><!-- /.navbar-collapse -->
</div><!-- /.container-fluid -->
</nav>
